# Model Library Plan

**Goal:** Register OpenAI / Anthropic / Ollama models and use the default for planning.

**Done:**
- [x] SQLite `models` store (keys masked in API)
- [x] `ModelClient` multi-provider chat
- [x] Planner uses library default (env OpenAI fallback)
- [x] API CRUD + default + test
- [x] `/models` UI
