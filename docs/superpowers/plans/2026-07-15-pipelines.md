# Pipelines Implementation Plan

**Goal:** Multi-step agent workflows with create/run/history over the kernel.

**Done:**
- [x] SQLite `pipelines` + `pipeline_runs`
- [x] Sequential `PipelineRunner` (`{input}`, `{previous}`)
- [x] FastAPI endpoints
- [x] Next.js `/pipelines` + detail/run viewer
- [x] Tests + docs
